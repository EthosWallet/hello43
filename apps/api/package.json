{
  "name": "@company/api",
  "version": "2.1.0",
  "description": "API backend service",
  "main": "server.js",
  "dependencies": {
    "express": "^4.18.0",
    "mongoose": "^6.0.0",
    
    // More dependency confusion opportunities
    "auth-middleware": "^1.3.0",
    "database-connector": "^2.5.0",
    "@company/shared-types": "^1.0.0",
    
    // Workspace reference (should be detected)
    "@company/frontend": "workspace:*",
    
    // GitHub repo jacking in API
    "custom-validator": "git+https://github.com/missing-dev/custom-validator.git",
    "api-helpers": "git+https://github.com/nonexistent/api-helpers.git"
  },
  "devDependencies": {
    "nodemon": "^2.0.0",
    
    // Internal tools that might not exist in public registries
    "company-linter": "^1.0.0",
    "internal-test-utils": "^0.9.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "mocha tests/"
  }
}
